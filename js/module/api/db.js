/**
 * Genereral Db Api of application,
 * database must be synchronized.
 *
 * @author kubasekA
 */
class DatabaseApi {

    constructor() {
    }

    /**
     * get item by id
     *
     * @param id
     * @return item
     */
    itemById(id) {
    }

    /**
     * get movie by id
     *
     * @param id
     * @return movie
     */
    movieById(id) {
    }

    /**
     * set item to db
     *
     * @param item
     */
    insertItem(item) {
    }

    updateItem(item) {
    }

    /**
     * set movie to db
     *
     * @param movie
     */
    insertMovie(movie) {
    }

    updateMovie(movie) {}

}

module.exports = DatabaseApi()



